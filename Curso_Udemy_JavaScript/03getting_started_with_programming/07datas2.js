console.log(19.9 * 0.6); //Usando valores literais.

let preco = 19.90;
let desconto = 0.4;
let precoComDesconto = preco * (1 - desconto);
console.log(precoComDesconto);

let nome = "Caderno";
let categoria = "Papelaria";

//Português
/*Abaixo estou usando a concatenação
cujo sinal é "+", e também coloquei strings
(texto) para ser mostrado na tela.*/

//English
/* Below I'm using concatenation
whose sign is "+", and I also put strings
(text) to be displayed on the screen.*/
console.log("Produto: " + nome 
+", Categoria: " + categoria
+", Preço: " + preco
+", Desconto " + desconto);