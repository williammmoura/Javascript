const a = 7;
let b = 3;

b += a; /**Equivalente -> b = b + a */
console.log(b);

b -= 4; /**Equivalente -> b = b - 4 */
console.log(b);

b *= 2 /**Equivalente -> b = b * 2 */
console.log(b);

b /= 2 /**Equivalente -> b = b / 2 */
console.log(b);

//Módulo
b %= 2 /**Equivalente -> b = b % 2 */
console.log(b);//valor par = 0.

